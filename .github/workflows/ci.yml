name: Backend NestJS Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  call-test-lint-workflow:
    uses: blimit-dev/infra-pipelines/.github/workflows/nestjs-pipeline.yml@main
    with:
      node-version: "24.4.1"
      branch: ${{ github.ref_name }}